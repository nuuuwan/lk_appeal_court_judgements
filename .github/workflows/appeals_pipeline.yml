name: appeals_pipeline

on:
  workflow_dispatch:
    inputs:
      max_dt:
        description: "max_dt"
        required: false
        default: "1"
  schedule:
    - cron: "30 6 * * *"

jobs:
  appeals_pipeline:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/nuuuwan/pdf-scraper-image:latest

    steps:
      - name: "⬇️ Checkout repo"
        uses: actions/checkout@v4
        with:
          ref: "main"
          token: "${{ secrets.REPO_DATA_TOKEN }}"

      - name: "⚙️ Run appeals_pipeline"
        run: |
          export PYTHONPATH="$PYTHONPATH:./src"

          git config --global user.email "${{ secrets.GIT_USER_EMAIL }}"
          git config --global user.name "${{ secrets.GIT_USER_NAME }}"

          git pull origin main --rebase
          python workflows/appeals_pipeline.py ${{ github.event.inputs.max_dt }}

      - name: "⬆️ Push repo"
        run: |
          git add .
          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi
          git commit -m "🤖 [appeals_pipeline.yml] $(date '+%Y-%m-%d-%H%M')"
          git pull origin main --rebase
          git push origin main
